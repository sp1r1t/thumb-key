## Suave 0.1.0

Initial Suave checkpoint.

- App identity: Suave (`com.suave.keyboard`), default layout S12
- Gesture engine and layout data model
- Brand B/W theme, flat launcher mark, setup flow polish
- Clipboard history on by default; auto-cleanup off by default
- Default key padding 0
- Settings database starts at schema v1
